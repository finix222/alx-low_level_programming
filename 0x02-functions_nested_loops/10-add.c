#include <stdio.h>
/**
* add - for returning absolut value of a number re
* @a: number to be absoluted
* @b: second number to be added
* Return: absoluted integert
*/
int add(int a, int b)
{
	return (a + b);
}
